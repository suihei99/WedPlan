// Placeholder for AI Budget page scripts
// Additional page-specific JavaScript can be placed here if you want to move inline scripts to a module.
// Currently the page uses inline scripts pushed via @push('page-scripts').

document.addEventListener('DOMContentLoaded', function () {
    // Example: expose a small helper for other modules
    window.AIBudget = {
        version: '0.1.0'
    };
});
